/**
 * Inventory seed data is created by the first-run setup wizard
 * (org, Super Admin, All Items, units, custom fields, reasons).
 * This file exists so `prisma db seed` is a no-op rather than a surprise.
 */
console.log("Primary WMS: use the /setup wizard to create the first Super Admin.");
